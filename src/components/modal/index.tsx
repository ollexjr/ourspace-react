import React from 'react';
import { createPortal } from 'react-dom';

const Modal = () => {
    /*return createPortal(
        <div>
            {"modal"}
        </div>,
        document.getElementById("modal"))*/
    return (<div></div>)
}